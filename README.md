rule-index.metta 
It contains the implementation of getProduction when the grammar includes non-terminal and index

prob.metta
It contains the implementation of getProduction when the grammar includes non-terminal, probability and index
